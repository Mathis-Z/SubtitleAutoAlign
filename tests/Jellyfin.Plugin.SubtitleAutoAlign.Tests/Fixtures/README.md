# Test fixtures

Drop sample subtitle/media files here for fixture-driven unit tests (e.g. golden-file
comparisons). Nothing currently in this directory is required for the unit test suite
to pass — the existing unit tests use synthetic data and a fake process runner, not
real files. Files added here are copied to the test output directory automatically.
