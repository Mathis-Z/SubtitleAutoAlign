# Test fixtures

The unit tests cover pure logic (file naming, argument building, change
detection, configuration) and fake the ffsubsync process, so they need no
media. The real movie/subtitle fixtures live in
`tests/Jellyfin.Plugin.SubtitleAutoAlign.IntegrationTests/Fixtures`.
